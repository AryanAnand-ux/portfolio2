# Environment Variables Example

# This portfolio is currently a static client-side app and does not require
# runtime environment variables for local development or production builds.
#
# If future features add API integrations, keep public client variables prefixed
# with VITE_ and never place private API keys or secrets in client-side env files.
